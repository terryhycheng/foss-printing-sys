import classNames from "classnames";
import styles from "./Header.module.scss";

const Header = () => {
  return <header className={classNames(styles.container)}>Header</header>;
};

export default Header;
